# Arrival_registration_table
Это приложение — **система учёта прибытия сотрудников** с графическим интерфейсом на Python (tkinter). Вот подробный обзор его функционала:

---

### 📌 **Основное назначение**
Программа позволяет **фиксировать время прибытия сотрудников**, сравнивать его с установленным временем прибытия, считать опоздания и вести учёт статусов присутствия.

---

## 🔧 **Основные функции и возможности**

### 1. **Управление временем запуска учёта**
- Кнопка **"ЗАПУСК"** — запускает таймер прибытия. Время запуска отображается вверху окна.
- Все опоздания считаются относительно этого времени.

---

### 2. **Добавление сотрудников**
- В правой панели есть форма для добавления сотрудника:
  - **Должность** (выбор из списка: Инженер, Менеджер и т.д.)
  - **ФИО** (вводится вручную или импортируется)
  - **Время прибытия** (выбор из фиксированных вариантов: 1 час, 1.5 часа и т.д.)
  - **Статус** (Присутствует / Болен / Командировка)
- Кнопка **"Добавить"** — добавляет строку в таблицу.

---

### 3. **Таблица сотрудников (основное окно)**
Столбцы:
- **№** — порядковый номер
- **Должность**
- **ФИО**
- **Время прибытия** (ожидаемое)
- **Факт** (фактическое время прибытия)
- **Опоздание** (разница между фактом и ожидаемым)
- **Статус**

**Цветовое кодирование строк:**
- 🟢 **Зелёный** — прибыл вовремя
- 🔴 **Красный** — опоздал
- 🔵 **Синий** — статус не "Присутствует" (болен, командировка)

---

### 4. **Взаимодействие с таблицей**
- **Двойной клик по строке** — фиксирует факт прибытия (если статус "Присутствует" и время запуска установлено).
- **Выделение строки** — заполняет правую панель её данными для редактирования.
- **Изменение данных в правой панели** — автоматически обновляет выделенную строку.

---

### 5. **Контекстное меню (правая кнопка мыши)**
- **Копировать** — копирует выделенные строки в буфер.
- **Вырезать** — копирует и удаляет выделенные строки.
- **Удалить** — удаляет выделенные строки.

---

### 6. **Импорт и экспорт данных**
- **Импорт ФИО из Excel** — загружает список сотрудников из первого столбца Excel-файла.
- **Экспорт в Excel** — выгружает таблицу в Excel с процентами прибытия за указанный период (1, 1.5, 2, 2.5, 3 часа).

---

### 7. **Статистика**
В правой панели отображается:
- Присутствует (общее количество)
- Болен
- Командировка
- Пришли (уже прибыли)
- Процент прибывших

---

### 8. **Управление данными**
- **Очистить данные** — обнуляет факт прибытия, опоздания и статусы.
- **Удалить строку** — удаляет выделенную строку.
- **Удалить всю таблицу** — полностью очищает таблицу (необратимо).
- **Автосохранение** — данные сохраняются в `data.json` при закрытии и изменении.

---

### 9. **Поиск**
- **Ctrl+F** — открывает диалог поиска по фамилии.

---

### 10. **Сортировка**
- Таблица автоматически сортируется по алфавиту ФИО.

---

### 11. **Горячие клавиши и скроллинг**
- **Ctrl+F** — поиск.
- **Колёсико мыши** — быстрая прокрутка таблицы.
- **Правая кнопка мыши** — контекстное меню.

---

## 📂 **Сохранение данных**
Все данные сохраняются в файл **`data.json`** в формате JSON, включая:
- Время запуска
- Список сотрудников
- Их статусы, время прибытия, факт и опоздание

---

## 🎨 **Интерфейс**
- Тёмная тема
- Крупный шрифт (удобно для презентаций)
- Адаптивное расположение элементов
- Подсветка важных кнопок (зелёные, красные, синие)

---

## ✅ **Итог**
Это **полнофункциональное desktop-приложение** для учёта посещаемости сотрудников, которое позволяет:
- Добавлять/удалять сотрудников
- Фиксировать время прибытия
- Анализировать опоздания
- Вести статистику
- Импортировать и экспортировать данные
- Управлять данными через интуитивный графический интерфейс

Программа подходит для использования на рабочих местах, собраниях, учебных курсах или любых мероприятиях, где важно контролировать своевременность прибытия участников. 
